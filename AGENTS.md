# ZIM RUGBY UNION â€” Session State

## Objective
Harden ZRU production CMS (Directus on Railway) + Next.js site (Vercel). Work is done solo (Ed chose to skip Rick/ag-comm dispatch).

## Production Architecture (current truth)
- **Site**: Next.js on Vercel, project `zimrugby`, prod URL `https://zimrugby.vercel.app`. Env on Vercel: `DIRECTUS_READ_TOKEN` (public reads, read-only) + `DIRECTUS_TOKEN` (admin writes) + `DIRECTUS_ADMIN_TOKEN` (preview+prod, static token).
- **CMS**: Directus (npm 12.2.0, packaged from `^12.1.1`) on Railway, service `zru-directus-cms` (`56b04e95-0f7b-490c-8723-e42847beba50`), project `b6660c61-cb33-4058-a4a6-a24e7697848b`, prod URL `https://zru-directus-cms-production.up.railway.app`.
- **DB is now Postgres 18** (migrated 2026-08-09, SQLite retired): managed Railway Postgres service `Postgres` (`7b511abc-ff53-4d9e-ae1a-f0dee529ec65`), volume `postgres-volume` (`873906e0-6c45-4170-9f93-d4f0bd9fe95b`), reachable ONLY at internal host `postgres.railway.internal:5432` (no public proxy). Connection: `postgres://postgres:ijavlSJayOFQyCjkLBLbvXcExSHxIoHF@postgres.railway.internal:5432/railway`.
- **Scheduled backups**: service `zru-pg-backup` (`2e72ea13-e33a-45ed-831b-be8735f89254`) runs image `postgres:18-alpine` on cron `0 2 * * *` (daily 02:00 UTC) â€” `pg_dump --format=custom` â†’ volume `zru-pg-backup-volume` (`c5a1cb5f-5cdf-4147-915d-0ca24e94a1dc`) mounted at `/backups`, retention 14 days, gated by min 5-min interval. Verified restorable (66 TABLE DATA entries, format 1.16). Dumps download via `railway volume files -v <vol> download /backups/zru-*.dump` â€” but SFTP only responds while a cron container is alive, so run during/near a scheduled fire.
- **Deployment source**: `railway up` from `directus-cms/` (staging copy WITHOUT `.env` â€” baked `.env` was overriding Railway vars and caused the whole incident). Railway env vars are now the single config source.
- **Start script**: `directus bootstrap && node restore-license.js && directus start` â€” bootstrap required for empty Postgres; `restore-license.js` replays the encrypted license_key+license_token from `ZRU_LICENSE_KEY_RESTORE`/`ZRU_LICENSE_TOKEN_RESTORE` env vars into `directus_settings` to avoid online re-activation (license maxed on fresh DBs).
- **Uploads now DURABLE**: `STORAGE_LOCAL_ROOT=/app/data/uploads` env var set (2026-08-09) â€” Directus writes/reads uploads on the existing volume `zru-directus-cms-volume-vtfd` at `/app/data/uploads`, so media survives redeploys. Verified: fresh API upload landed on `/app/data/uploads`; baked `/app/uploads` unchanged (75 files). All 75 existing upload files copied into volume. Deploy stage no longer ships `uploads/` (dead weight). NOTE: `UPLOADS_LOCATION` is NOT a valid Directus 12 var (ignored â€” writes still went to `./uploads`); use `STORAGE_LOCAL_ROOT`.
- **Pre-existing uploads data quirk â€” RESOLVED 2026-08-09**: all 20 records in `directus_files` referenced files NOT on disk (0 of 75 on-disk files were referenced by any content; site images are external URLs / local static paths). Deleted all 20 orphan records via API (verified `directus_files`=0). Pre-cleanup backup: `%TEMP%\opencode\zru-prod-backup\data.db.PRE-FILE-CLEANUP`. Uploads still work on the durable volume (verified new upload â†’ `/app/data/uploads`).
- Admin user: id `33a06c7a-97fd-49d7-8236-2004a21fee4e`, email `admin@zimrugby.co.zw`, role `efa16710-b30a-4a4a-9f11-be82c159804d`, active.

## Rotated Secrets (CURRENT â€” old leaked values are DEAD)
- NEW static token: `zru-directus-admin-bd92e3c6572c02320b494e2bfa5f9d888d780879debdae60` (in `%TEMP%\zru-new-token.txt`)
- NEW admin password: `Zru-210661a9eff602b8d062b5e3!` (in `%TEMP%\zru-secrets.json`)
- NEW KEY: `a60076331296bc5ef0cc5ca945c27187`; NEW SECRET: `00149f001afe3708151bf6e3603531ea6e36887b995a3cdc`
- OLD leaked token `zru-directus-admin-8afea6eb598749eb88e651ac` and OLD password `ZimRugbyUnion2027!` now return 401. Verified all 4 checks PASS on 2026-08-09 ~09:30.

## Railway env vars (set on service, verified)
ADMIN_EMAIL, ADMIN_PASSWORD (new), KEY, SECRET (new), DB_CLIENT=pg, DB_HOST=postgres.railway.internal, DB_PORT=5432, DB_USER=postgres, DB_PASSWORD=ijavlSJayOFQyCjkLBLbvXcExSHxIoHF, DB_DATABASE=railway, STORAGE_LOCAL_ROOT=/app/data/uploads, PUBLIC_URL, CORS_ENABLED=true, CORS_ORIGIN=https://zimrugby.vercel.app,http://localhost:3000, RATE_LIMITER_ENABLED=true, CACHE_ENABLED=true, CACHE_STORE=memory, CACHE_TTL=600, LOG_LEVEL=info, LICENSE_KEY=DXJM1-2PR3X-9QXDC-83QHS-XHMAA, ZRU_LICENSE_KEY_RESTORE, ZRU_LICENSE_TOKEN_RESTORE, PORT=8055. NOTE: DB_FILENAME no longer set (SQLite retired).

## Lessons / Traps (IMPORTANT)
1. **NEVER mount a Railway volume at `/app`** â€” shadows image app code â†’ `ENOENT /app/package.json` crash. Mount at subpath like `/app/data`.
2. **Do NOT ship a `.env` in `railway up` source** â€” Directus's env loader lets the baked file override Railway process env. Keep secrets ONLY in Railway env vars.
3. `railway redeploy` re-runs the LATEST deployment (env vars apply); `railway service restart` fails with "Deployment is not restartable". Use `railway up` for a fresh deployment.
4. `railway up` in an unlinked folder creates a NEW project â€” always pass `-p <projectId> -e production -s <serviceId>` or link first.
5. Uploads/media now live in the durable volume at `/app/data/uploads` (`STORAGE_LOCAL_ROOT`) â€” deploy source no longer ships `uploads/` (dead weight). Directus storage root env var is `STORAGE_LOCAL_ROOT`, NOT `UPLOADS_LOCATION`.
6. **Postgres bootstrap**: `directus start` on an EMPTY Postgres crashes (no tables). Start script MUST be `directus bootstrap && directus start`. License: fresh DB has no license_token so Directus tries online activation â†’ `FATAL: Activation limit exceeded`. `restore-license.js` replays the encrypted key/token into `directus_settings` so it takes the verify path instead.
7. **Postgres has no public proxy** â€” `postgres.railway.internal` only resolves inside Railway. No local pg_dump/psql; backups run in-network via the `zru-pg-backup` cron service. pg_dump client version must match server (18.x) or you get "server version mismatch".
8. **Railway cron services** (e.g. backups) only spin up at the scheduled time â€” SFTP/volume-file access only works while a cron container is alive. Volume backup APIs (`volumeInstanceBackup*`) are paid-gated on Hobby â€” use a cron service instead.
9. **Railway GraphQL gotchas**: `railway variable set "K=V"` (not `variables --set`); cron `*/5` is the minimum interval (error "Minimum interval between cron executions is 5 minutes"); `serviceInstanceUpdate` returns Boolean (no selection set allowed); pass `-s <serviceId>` with `-p` + `-e production` for non-linked service queries.
10. **`.vercelignore` globs are case-insensitive on Windows** â€” the old `Gallery/` rule silently excluded `public/images/gallery/` from every deployment (hero + gallery images 404'd). Anchored: `/Gallery/`. Check .vercelignore before adding public/ asset folders.

## Backups
- **LIVE (new)**: daily `pg_dump --format=custom` via `zru-pg-backup` cron service (02:00 UTC) â†’ `zru-pg-backup-volume`, 14-day retention. Verified restorable 2026-08-09 (66 TABLE DATA entries). No local copy needed; restore = spin a PG 18 container + `pg_restore`.
- `%TEMP%\opencode\zru-prod-backup\data.db.PRE-POSTGRES-MIGRATION` (1,716,224 B) â€” verified pre-migration SQLite backup (integrity ok), kept as rollback artifact.
- `%TEMP%\opencode\zru-prod-backup\` â€” older SQLite backups for reference only (SQLite retired).
- Note: `grassroots_initiatives` was RESTORED live on 2026-08-09 (created via Directus API POST /collections with fields atomically). Schema matches site expectations: id(PK auto-inc), title, badge, subtitle, description, stat, stat_label, image, link, sort, status(default 'published'). Seeded with 3 published items (mapped from FALLBACK_INITIATIVES). Admin-token and public-anon reads both return 200/3. Site `zimrugby.vercel.app` 200.

## Local
- `directus-cms/.env` â€” updated to NEW creds (admin password, KEY, SECRET) on 2026-08-09.
- `directus-cms/data.db` â€” last SQLite copy (SQLite retired 2026-08-09; Postgres is now the live DB, no local DB file).
- `directus-cms` is a git repo (init 2026-08-09). `.gitignore` excludes `.env`, `data.db`, `uploads/`, `node_modules/`, `*.log`.
- `DEPLOY.md` (repo root) â€” full deploy runbook: staging copy build, `railway up` flags, volume download, local boot, DO-NOT list. Also referenced from `directus-cms`.
- `ZimRugby` is its own git repo (remote `No1zee/ZimRugby`, branch `preview`). `ZimRugby/.env.local` â€” `DIRECTUS_READ_TOKEN` = read-only token (line 8), `DIRECTUS_TOKEN` = NEW admin token (line 9); `VERCEL_OIDC_TOKEN`; `ADMIN_PASSWORD` = site admin portal password. Do not commit.
- **Read-only token** (2026-08-09): `zru-directus-readonly-0e5e7a2e04f64c9b847ad2b9c7c0b5e6` for user `site-reader@zimrugby.co.zw`, role "Site Reader", policy "Site Read-Only", linked via `/access`. Frontend `directusFetch`/`directusCount` use `DIRECTUS_READ_TOKEN` (fallback `DIRECTUS_TOKEN`); writes still use `DIRECTUS_TOKEN`. Verified: reads all 9 content collections, writes/edits blocked (403).
- **Site admin portal (2026-08-09)**: `/admin-login` â†’ `/admin`, auth migrated to **Supabase Auth** (Path A, aligns with blueprint). Login POSTs to `/api/admin/auth` â†’ `supabase.auth.signInWithPassword` (SSR server client); roles come from `app_metadata.role` (NOT env passwords). Users: admin@=super_admin, editor@=editor, media@=media_manager, auditor@=viewer (all @zimrugby.co.zw) + `edwardmagejo@gmail.com`=super_admin (password set to `zru-admin-2026`). `requireAdmin()` (server) + middleware now protect `/admin` (307 â†’ `/admin-login`), session refreshed via `@supabase/ssr`. Old env-password IAM (`zru_admin_auth` HMAC cookie, `signAdminToken`/`verifyAdminToken`/`validateCredentials`) REMOVED. `iam.ts` keeps `isAdminRole`, `roleToName`, audit log, `hasPermission`. NOTE: Vercel env vars show blank in dashboard/API because they're `type: sensitive` (masked) â€” they ARE populated (login 200 + CMS content served proves it). Deployed to prod commit `c2fd98c` (preview), verified live. Site passwords (OLD env-IAM values, now only used to match Supabase user passwords) in `%TEMP%\zru-site-admin-secrets.json`.
  - **Login quirk (verified 2026-08-11)**: `admin@zimrugby.co.zw` / `zru-admin-2026` works (super_admin). `edwardmagejo@gmail.com` with `zru-admin-2026` returns "Invalid credentials" â€” password differs; use the admin@ account for API/smoke tests.
- **Supabase admin users** created via Admin API (service_role key in Vercel `SUPABASE_SERVICE_ROLE_KEY`). All 4 + edwardmagejo confirmed with `email_confirm: true`. Fan zone users remain on same Supabase project (no role = 403 on /api/admin/auth).
- `directus-cms/revoke-public-pii-reads.js` â€” public read revoked on `fan_zone_members` + `onboarding_submissions` (local DB verified; prod DB has those collections with 0 members).

## Next Move
- Done (2026-08-09): uploads volume-backed (`STORAGE_LOCAL_ROOT`), local dev synced to new creds, git enabled, deploy runbook written, orphan `directus_files` records purged (files=0). Site admin portal live on prod with env-based IAM (8 env vars, `vercel --prod` from `preview`).
- Done (2026-08-09 pm): admin auth migrated to **Supabase Auth** (Path A) â€” 4 admin users + edwardmagejo(super_admin) in Supabase with `app_metadata.role`; `requireAdmin()`/login/check routes use SSR server client; middleware protects `/admin` â†’ `/admin-login`. Commit `c2fd98c`, deployed & verified live.
- Done (2026-08-09 evening): **SQLite â†’ Postgres 18 migration complete on prod**. All content migrated (news=20, matches=9, players=20, campaigns=4, school=3, grassroots=3, announcements=1, pages=1, page_sections=2). Bootstrap + license-restore fix in start script. Read-only token wired into site reads (deployed `c908bd4`). Scheduled daily backups live (`zru-pg-backup`, verified restorable).
- Done (2026-08-11): **lookup collections restored on prod** â€” teams (5), opponents (9), competitions (4), venues (6) were NOT part of the 08-09 migration and were re-seeded from `%TEMP%\opencode\zru-prod-backup\data.db.PRE-POSTGRES-MIGRATION` with their ORIGINAL uuid ids, so matches link to real team names again and the admin fixture form has working dropdowns. Verified: public reads return 5 teams, match `team_id` values resolve.
- Done (2026-08-11): **admin CRUD fixes deployed** (`a4d2eb5`, preview â†’ Vercel prod): `/api/admin/directus` auto-injects a UUID on create for text-PK collections (`announcements`, `matches`, `pages`, `teams`, `opponents`, `competitions`, `venues` â€” Directus can't auto-generate these, creates previously 400'd); MatchCentrePanel now requires kickoff + auto-slugs + sends `match_type`; CollectionManager status toggle is boolean-aware (announcements `is_enabled`); CampaignsPanel is now full CRUD via CollectionManager (was read-only with a Directus "Data" link).
- **KNOW YOUR COLLECTIONS**: text-PK (uuid, need client id): announcements, matches, pages, teams, opponents, competitions, venues. Integer auto-PK: news, events, campaigns, faqs, programmes, grassroots_initiatives, footer_navigation, players, partners. `matches` also requires (NOT NULL, no default): slug, match_type, team_id, opponent_id, kickoff_at.
- Done (2026-08-11): **admin usability overhaul + Teams & Squads tab** (`93cd63f`, preview â†’ Vercel prod, live-verified): new "Teams & Squads" tab (Matches section) with full CRUD for teams/opponents/competitions/venues via CollectionManager; new shared UI (`ui/ToastProvider.tsx` `useToast`, `ui/ConfirmProvider.tsx` `useConfirm`, `ui/CollapsibleSection.tsx` accordion + unsaved-changes guard); CollectionManager progressive-disclosure add form + Newest/Oldest sort + toast/confirm feedback + dirty reporting; ArticleComposer / MatchCentrePanel / EventsPanel migrated to toasts + confirm modals (no more window.confirm / inline banners); TodayOverview now shows a Recent activity feed (Directus `/activity`); news admin cap raised 100 â†’ 500; AdminClient wrapped in Toast/Confirm providers with unsaved-changes guard on tab switch + beforeunload; editor role granted Teams tab in `iam.ts`, `legacy-roles.ts` and new migration `supabase/migrations/20260811120000_editor_teams.sql`. Live CRUD round-trip verified against prod Directus (venue create 200 / patch 200 / delete 204).
- **DONE (2026-08-11)**: editor `admin_roles` row updated via Supabase SQL Editor (migration `20260811120000_editor_teams.sql`) â€” verified live via editor login: tabs `["overview","media","fixtures","teams"]`, teams grants read/create/update.
- **DONE (2026-08-11)**: **news not appearing was a BROKEN PROD ENV VAR, not data** â€” Vercel `DIRECTUS_READ_TOKEN` had a literal trailing `\nn` baked into the value, so EVERY public Directus read threw `Headers.append: "Bearer â€¦\nn" is an invalid header value` and silently fell back to static/empty content. Fixed via `vercel env rm` + `env add` with the clean token (line 8 of `ZimRugby/.env.local`, value `zru-directus-readonly-0e5e7a2e04f64c9b847ad2b9c7c0b5e6`), then `vercel --prod` redeploy. Verified live: homepage now renders the 2 new articles + all old ones; zero "Failed to fetch from Directus" errors in fresh logs.
- **DONE (2026-08-11)**: **migration gap found + fixed** â€” the 08-09 SQLiteâ†’Postgres migration only covered 9 collections. Comparing vs `data.db.PRE-POSTGRES-MIGRATION` showed 6 collections EMPTY in Postgres: `hero_slides` (3), `partners` (5), `match_centre_settings` (1), `standings_tables` (1), `global_settings` (1), `events` (6) = 17 rows. All restored via Directus API with ORIGINAL ids (admin token). Full parity now: news 20â†’22 (expected +2 new articles), matches 9, players 20, campaigns 4, grassroots 3, school 3, announcements 1, pages 1, page_sections 2, teams 5, opponents 9, competitions 4, venues 6, hero_slides 3, partners 5, match_centre_settings 1, standings_tables 1, global_settings 1, events 6, social_posts 0, faqs 0. `programmes`/`footer_navigation` 403 on read token (not in Site Reader policy) but empty in backup anyway. Scripts: `%TEMP%\opencode\mig-compare.py`, `%TEMP%\opencode\mig-restore.py`.
- **DONE (2026-08-11)**: **maintainability tooling deployed** (`1ed4e69`): `.github/workflows/ci.yml` (tsc + `npm test` on push/PR to preview), `scripts/smoke-test.mjs` (`npm run smoke`, 10/10 PASS incl. editor-drift check), vitest (20 tests: `iam.test.ts`, `status.test.ts`). Also **events status bug fixed**: `deriveEventStatus` `eventEnd` now end-of-start-day (was start+24h â†’ events with a time stayed "Today" into the next day).
- **DONE (2026-08-11)**: **static fallbacks now work in prod** (`fe4db75` + `b860c8b`): the old `readStaticJson` read `public/data/*.json` via `fs` â€” which ENOENTs in Vercel serverless (`/var/task/public/data/...`) and the VERCEL_URL fallback fetched the HTML index page â†’ JSON parse error â†’ empty sections. New `src/lib/static-data.ts` imports matches/reports/social JSON **at build time** (bundled, always readable on any runtime); `readStaticJson` returns the bundled copy. Also `getLatestReports()` is now **news-collection-first**: live `news` (published, sort -date) + announcements, with bundled `reports.json` only as the Directus-down fallback (stale > empty). Result: `/media` shows ALL news incl. newly posted articles (detail pages too), homepage hub unaffected (same `news-<id>` scheme, dedupe intact). Deployed, live-verified. NOTE: read-token policy still lacks `clubs` (build log 403 â†’ local fallback, harmless).
- **DONE (2026-08-11)**: **on-demand ISR revalidation via Directus webhook** (`9c00aef`, deployed): every `directusFetch`/`directusCount` call now tags its fetch cache `directus:<collection>` (src/lib/directus/fetch.ts); new route `src/app/api/revalidate/route.ts` purges the tag (Next 16 requires a cache-life profile arg â€” uses `"minutes"`). Directus side: **event flow** "Revalidate site cache" (flow id `03eeb193-ac1c-4fea-9fc4-a33bfe6b57c9`, op id `4e6c488e-01ed-4d25-aa26-36588ddccfb2`, type=action, scope items.create/update/delete, 21 content collections) POSTs `{"collection":"{{$trigger.collection}}"}` to `https://zimrugby.vercel.app/api/revalidate` with `Authorization: Bearer <REVALIDATE_SECRET>` (value in `%TEMP%\zru-revalidate-secret.txt`; env var also on Vercel prod). Note: legacy `/webhooks` route 404s in Directus 12 â€” use flows. End-to-end verified live: patched a news title â†’ flow fired (activity `run`) â†’ /media + homepage showed the new title within ~2 min (was 300s). REVALIDATE_SECRET also added to `ZimRugby/.env.local` (do not commit). Manual check: `POST /api/revalidate` with bearer â†’ 200 `{"revalidated":true}`, wrong token â†’ 401 (verified 2026-08-11).
- **DONE (2026-08-11, code; AWAITS Upstash creds)**: **KV cache layer** (`dfc5c85`, deployed, inert until enabled): new `src/lib/cache.ts` (Upstash Redis, graceful no-op without `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN`, 1.5s timeout) + `src/lib/directus/fetch.ts` now WRITES successful `directusFetch`/`directusCount` results to KV (key `directus:<coll>:sha1(params)`, TTL 1h) and on any Directus failure SERVES the last-known-good payload from KV before falling back to bundled static data. Also added `DIRECTUS_API_URL` server-side override (reads only) â€” set it to a dead URL to simulate an outage for testing, no rebuild needed (runtime env var). TO ENABLE: Ed creates a free Upstash Redis DB (console.upstash.com) â†’ `vercel env add UPSTASH_REDIS_REST_URL/TEST` â†’ done, no redeploy (runtime vars). Alternative if no Upstash: Supabase `site_cache` table + service key (needs `SUPABASE_SERVICE_ROLE_KEY` locally + SQL editor table).
- **DONE (2026-08-11)**: **hero images fixed â€” root cause was `.vercelignore`, not content** (`0c31ced`, deployed + live-verified): the unanchored `Gallery/` rule case-insensitively excluded `public/images/gallery/` from EVERY Vercel upload, so all gallery images 404'd (incl. the hero slide referencing the battle-of-zambezi webp). Anchored to `/Gallery/` in `.vercelignore` + `.gitignore`. Also patched `hero_slides` rows in Directus to local paths (`/images/gallery/zimbabwe-sables-battle-of-zambezi-gameday1-505.webp`, `/images/hero/lady-sables.webp`, `/images/hero/zim-u20s.webp`). Verified live: gallery image 200, all 3 slides render local images in homepage HTML.
- **DONE (2026-08-11)**: **admin portal usability overhaul** (`c1f2bff` + `3586d12`, deployed + live-tested 10/10 smoke): CollectionManager now has status filter chips (All/Published/Draft with counts), bulk actions (row checkboxes + select-all page â†’ Publish/Move to draft/Delete, confirm-gated), saving state (disabled + spinner, kills double-post), inline required-field validation with focus, Undo-delete toast (10s, re-creates item), Duplicate action ("(copy)" title, blank slug, draft status), publish-date defaults to today, editing chip shows item name instead of raw uuid. `/api/admin/directus` PATCH/DELETE accept an `ids` array for bulk ops (Directus `{keys, data}`). Dashboard "Review" on drafts deep-links into the article editor (`tab-events.ts` now carries an `openItem` intent; AdminClient passes `focusId` to the news CollectionManager). Smoke suite gained a permanent admin bulk round-trip (login â†’ create 2 â†’ bulk publish â†’ CMS-verify â†’ bulk delete â†’ CMS-verify). NOTE: `src/app/gallery/layout.tsx` (gallery metadata layout, untracked before) was committed with `c1f2bff` via `git add -A`.
- **DONE (2026-08-16)**: **Phase 1 â€” soft delete, trash, restore, purge gating, audit** (`7e4c145`, preview â†’ prod, live-verified): all **33** content collections got `deleted_at`/`deleted_by` (via `scripts/add-soft-delete-fields.mjs`); `/api/admin/directus` DELETE now soft-deletes (hard:true purge stays super_admin-only; editor 403), GET hides trashed, PATCH keeps the same id; new `/api/admin/directus/trash` route (list/restore/purge); CollectionManager undo â†’ restore SAME row (links survive, the old new-UUID bug is dead) + Trash-panel UI + datetime field type + honest delete copy; WS1 announcements form matches prod schema (numeric priority, start/end, scope, variant, CTA, badge, sticky) + canonical `announcements` type. Audit trail: the Supabase `audit_logs` migration was never applied on the live project, so the audit layer was repointed to Directus `audit_log` collection (created via API + least-priv read/write policy for the token role; system-field `date_created` sort 403s â†’ added own `created_at` timestamp field for deterministic sorting). Verified: tsc/lint clean, 27/27 unit tests, build green, `scripts/phase1-e2e.mjs` **12/12** (createâ†’publicâ†’patchâ†’audit CREATE+UPDATEâ†’soft deleteâ†’trash has SAME idâ†’restoreâ†’editor purge 403â†’super_admin purgeâ†’unauthenticated trash 401).
- **DONE (2026-08-16)**: **Top announcement ribbon now flips through headlines** (`2013abf`, preview â†’ prod, live-verified): the original `GlobalAnnouncementBar` (in `Navigation.tsx:202`, drops over the utility bar; red critical / green high / neutral normal gradients, shimmer, spring, glow, badge/CTA/dismiss) was repurposed from showing a single global banner to **rotating every active global-scope announcement + campaigns** (6s flip via AnimatePresence mode="wait", `n/total` counter, hover-pause, per-item dismiss via localStorage). BREAKING/critical first with the red pulse. Campaign feed = public `GET /api/campaigns` route (created earlier for the rejected BreakingNewsBar, kept). `src/components/ui/BreakingNewsBar.tsx` and its layout mount were **reverted** (a `from-red-950/95 via-zru-green/95 to-red-950/95` trial did not match the original design); ticker marquee `AnnouncementsTicker` below the nav unchanged. Verified live via headless Chrome DOM: drops in red `[BREAKING] sables tesr!!!` `1/3`, then flips across moods; tsc clean, 27/27 tests, build green.
- **DONE (2026-08-16)**: **WS4 â€” scheduled announcements admin** (`d7f1b1a`, `4120092`, `30ded2b`, `79f0f6f`; preview â†’ prod, live E2E ALL PASS): CollectionManager gained optional `scheduleField {starts, ends}` â†’ Upcoming/Active/Expired chips with per-state counts + per-row window chip and date-range label (`15 Aug â€“ 18 Aug`); `StatusChip` extended with schedule states; **super_admin-only "Purge expired"** confirm-gated action moves every date-window-expired announcement to trash (recoverable); wired on the Banners & announcements manager. Root causes fixed along the way: (1) admin page sorted `announcements` by system `date_created` which the site read token can't access (403 â†’ admin list silently empty) â€” added own `created_at` timestamp field (default `$NOW`, hidden) via Directus API + backfilled 7 rows + switched sort to `-created_at` (read-token readable, verified); (2) admin write routes (`/api/admin/directus` + `/trash`) now fire the fetch-cache tag purge (`/api/revalidate`, REVALIDATE_SECRET) on every successful create/patch/delete/restore/purge â€” admin lists refresh instantly instead of racing the Directus flow inside the 60s cache window; (3) announcements `scope` is a Directus CSV field â€” the WS1 form sent a plain string (create 500'd) â€” new `csv` field type in CollectionManager splits on commas on submit and joins arrays on edit-prefill ("global, homepage, media" â†’ `["global","homepage","media"]`, live-verified via the real form). Live verification: seeded 3 windowed announcements â†’ toolbar counts exact (6/1/2/1) â†’ per-row chips match windows â†’ EXPIRED filter isolates expired â†’ purge â†’ EXPIRED (0)/PURGE EXPIRED (0) + trash contains the row â†’ trash-purge cleanup â†’ zero leftovers. 27/27 tests, tsc clean, build green. Only DB change: `announcements.created_at` (API-created, no migration file).
- Done (2026-08-16): **announcement delivery + analytics roadmap decided and documented** â€” blueprint Â§20 (FUTURE ROADMAP â€” FAN MESSAGE DELIVERY & ANALYTICS MACHINE WS5/WS6) written into `ZIM RUGBY UNION\BLUEPRINT.md` (workspace doc, NOT in any git repo â€” don't commit). Decisions: dismissal-aware endpoints (`GET /api/fan/feed`, `POST /api/fan/messages`, `POST /api/events`), `zru_anon_id` cookie (1yr), Supabase `fan_message_state` + `events` + `daily_metrics` rollup, segment targeting upstream (`segment`/`audience_scope` fields finally used), admin Message Stats panel (impressions/dismissals/CTR/segments) â€” all WS5. Traffic analytics = Umami self-hosted on Railway (MIT/free/cookieless) WS6, PostHog cloud free tier as optional lens. None scheduled â€” future only.
- **DONE (2026-08-16)**: **admin role-gating hardened â€” server-side panel gate + least-privilege data loading** (`975bbfa`, preview â†’ prod, live RBAC E2E **62/62 PASS**): new `requirePanel(tab)` in `src/lib/admin/auth.ts` enforces `canAccessPanel` server-side (tab list AND feature flag, mirrors client nav gate). Applied to: `/api/admin/audit-logs` GET+POST (was `requireAdmin` only â€” any admin could read security logs; now `audit_logs` panel-gated = super_admin in practice), `/admin/[slug]` page builder (was `requireFeature("pages_builder")` only â€” now also requires `pages` tab). `/admin` dashboard server page now fetches collections role-scoped (least privilege): `news`/`announcements`/counts only when `media` tab, `matches`/`standings`/counts when `fixtures`, `teams`/`opponents`/`competitions`/`venues`+lookups when `teams`|`fixtures`, `hero_slides`/`partners`/`referee_resources` when their tabs, `pages`/sections/activity when `pages`|`overview`, campaigns when `campaigns`|`overview` â€” viewer/editor no longer receive non-granted collection payloads serialized in HTML (verified: `hero_slides`, `referee_resources`, `grassroots_initiatives`, "News articles", "Banners & announcements" absent from editor/viewer HTML). Live check: editor nav = Today/News & Articles/Fixtures & Scores/Teams & Squads (12 panels denied), viewer = Today/Fan Zone/Enquiries + audit-logs 403, super_admin = all 18 + audit-logs 200, editor `/admin/about` redirects to `/admin`. tsc clean, 27/27 tests, build green, lint = pre-existing 7 errors only (none in touched files).
- **DONE (2026-08-16)**: **`/clubs` now serves LIVE CMS data â€” collection + read grant + dynamic render** (`0aa53cd`, preview â†’ prod, live E2E **9/9 PASS**): `clubs` collection created on prod Directus via API (fields: name, slug, province, league, venue, color, contact, description, status default published, sort; id auto-increment PK) and seeded with the 6 original fallback clubs (Old Hararians RFC, Harare Sports Club, Old Georgians RFC, Old Miltonians RFC, Matabeleland Warriors, Mutare Sports Club, sort 1â€“6). Read grant added to Site Read-Only policy (`60e59c08-30d5-4246-a260-4d98d8493a52`) â€” permission id 37, `{action: read, fields: ["*"], permissions: {}}` (read token was 403 before). TWO root causes chased to ground: (1) `/clubs` page had no render mode (no `revalidate`/`force-dynamic`) so it was statically prerendered at build with the fallback baked in â€” cache stayed STALE forever; added `export const dynamic = "force-dynamic"`. (2) The NEW `clubs` collection was created AFTER Phase 1, so it lacked `deleted_at`/`deleted_by` soft-delete fields â€” `directusFetch` universally appends `deleted_at: {_null: true}`, Directus 403'd on the unknown field, `getClubs()` silently fell back to `fallbackClubs`. Added both fields (+ m2o relation `deleted_by` â†’ `directus_users`) via API â€” identical queries went 403 â†’ 200/6. Revalidation confirmed already wired: Directus flow "Revalidate site cache" was updated with `clubs` in its 21-collection scope (flow `03eeb193-ac1c-4fea-9fc4-a33bfe6b57c9`). Live proof via Playwright (`ws5-clubs-e2e.cjs`): created `WS5 CLUBS PROBE` item â†’ visible on `/clubs` (7 cards, 9/9 PASS) â†’ probe deleted (204) â†’ revalidate fired â†’ probe gone, 6 clubs live from CMS. Helper script `%TEMP%\opencode\zru-clubs.py` (modes all|create|seed|grant|verify|probe|cleanup). No DB/migration file â€” all Directus API.
- **DONE (2026-08-17)**: **WS1 â€” opponents age-grade taxonomy + fixture categorization** (`dc655bc`, preview â†’ prod, live-verified): teams form `team_type` options fixed to match prod reality (added `u20`, `u18`, `development` â€” the old list had `age_grade`/`club` but NOT the `u20`/`development` values actually used in prod); opponents form `team_type` extended with age-grade categories (`["international","club","province","tour","u20","u18"]`). Data backfilled on prod: all 11 opponents got `team_type` (Tunisia U20/Botswana U20/Kenya U20 â†’ `u20`, the 8 senior sides â†’ `international`), Zimbabwe U20 team got `age_grade="U20"`, and both U20 matches got `match_type="age_grade"` (were wrongly `international`). Fixture create form now DERIVES `match_type` from the opponent's `team_type` (u20/u18 â†’ `age_grade`, else `international`) instead of hardcoding. Surfacing: `MatchCardViewModel` gained `opponentCategory` (from `oppObj.team_type` in `src/lib/match-centre/api.ts`); public `MatchCard` shows an "OPP u20" chip next to the competition for non-international opponents; admin fixture rows show an "u20 opponent" chip + the opponent dropdown labels age-grade sides (`Kenya U20 â€” u20`); admin fixture search now matches `opponentCategory`. `LookupOption` in AdminClient/page.tsx carries `teamType`. Verified: tsc clean, 27/27 tests, build green, phase1-e2e 12/12, lint clean, live payload contains both `international` and `u20` categories.
- **DONE (2026-08-17)**: **WS2 â€” news publish_at/expire_at scheduling** (`2899232` + `13c76d4`, preview â†’ prod, live E2E **10/10 PASS**): prod `news` collection gained `publish_at` + `expire_at` timestamp fields (via Directus API); all 21 existing rows backfilled `publish_at` from their `date`. Admin news manager wired with `scheduleField {{starts:"publish_at", ends:"expire_at"}}` + datetime form fields â†’ Upcoming/Active/Expired chips, per-row window labels, super_admin "Purge expired" (reuses WS4 machinery). Public visibility enforced at QUERY TIME via new `newsVisibilityFilter()` in `src/lib/api/news.ts` (status published AND `publish_at` null|â‰¤now AND `expire_at` null|â‰¥now) â€” applied in `getNewsArticles`, `getLatestReports` (data-fetcher), `getReportById` slug lookup, and the RSS feed. E2E (`scripts/ws2-news-schedule-e2e.mjs`, new permanent script): create future-scheduled â†’ hidden on /media + feed.xml â†’ patch to past â†’ visible â†’ set past expire_at â†’ hidden again â†’ admin API still lists the row â†’ cleanup. IMPORTANT LESSON: the Directus "Revalidate site cache" flow purge takes ~15-20s to propagate (CDN/ISR), not instant â€” E2E polls must allow 60s; also verify "hidden" BEFORE deleting the probe or you get a false pass. tsc clean, 27/27 tests, build green, phase1-e2e 12/12 (regression). No DB migration file â€” fields API-created.
- **DONE (2026-08-17)**: **Partners consolidation â€” /partners page now CMS-driven** (`0aeac6c`, preview â†’ prod, live-verified): the audit's "stub panels" finding was already stale â€” HeroLayoutPanel + SponsorsPanel already had full real CRUD on `hero_slides`/`partners` (persisting, honest toasts), the click-tracking route already used `partners` (not `sponsors`), AnnouncementsTicker was mounted, and the homepage SponsorGrid already read `getPartners()`. The REAL remaining gap: the public `/partners` page still rendered hardcoded `partnerData` from `src/data/partners.ts`, and prod `partners.tier` held legacy `"1"`/`"2"` values while the admin SponsorsPanel groups by `title/gold/silver/bronze` â€” so the admin panel showed ALL tier groups empty (5 partners invisible). Fixed: (1) backfilled prod tiers via API â€” Nedbank/Rugby Africa/World Rugby â†’ `title`, ZOC/SRC â†’ `gold` (legacy values mapped via `normalizeTier()` aliases `1â†’title, 2â†’gold, 3â†’silver, 4â†’bronze` in `src/lib/api/partners.ts`); (2) `getPartners()` now fetches + maps `tier` (canonical `PartnerTierKey`), MOCK_PARTNERS updated; (3) `/partners` page rewritten as async server component (ISR `revalidate = 300`) grouping live partners into PRINCIPAL/GOLD/SILVER/BRONZE sections via `PartnerTierSection`/`PartnerLogo` (now typed on the API `Partner`, dead `src/data/partners.ts` deleted); (4) fixed pre-existing logo fallback bug: `getFallbackLogo` key `src` never matched "Sports and Recreation Commission" (name doesn't contain "src") â†’ SRC logo silently fell back to `zru-logo.svg` on BOTH the homepage grid and /partners; key renamed to `sports and recreation`. Verified: tsc clean, 27/27 tests, build green (route table `â—‹ /partners`), lint = pre-existing 7 errors only (none in touched files), live: both tier sections + all 5 logos render from CMS. NOTE: Directus memory cache served stale GETs for ~10s after PATCHes during verification â€” re-GET or wait when checking writes.
- Remaining (optional, not started): custom CMS domain (Ed parked it â€” no new hostname for now). Queued next: WS2 follow-ups already done; remaining admin improvements + calendar SSoT decision (Supabase vs Directus) + stub-panel consolidation (hero_slides/partners) per ADMIN-AUDIT-PLAN.md.


## Session 2026-08-17 — Calendar SSoT (Directus) DONE — commit 2612cc6
- src/lib/calendar/occurrences.ts: unified occurrence stream — event_occurrences (events, published+public), raw matches (kickoff_at = Africa/Harare local wall time), announcement windows (non-ticker), campaign windows. CalendarOccurrence + occurrencesOnDay() + CAT helpers (CAT_TZ, 	oCatWallTime, kickoffToUtc).
- src/lib/calendar/ics.ts: single RFC 5545 generator — zoned DTSTART;TZID=Africa/Harare wall time, VALUE=DATE all-day, STATUS CONFIRMED/TENTATIVE/CANCELLED, SEQUENCE, UID zru-{source}-{id}@zimrugby.org. Both /api/calendar + /api/calendar.ics now serve it (Cache-Control public s-maxage=3600 — add ?cb= to bypass CDN when testing).
- getEvents() rewired onto the stream: time now populated from earliest non-cancelled occurrence; cancelled flag on EventItem; events without occurrences excluded (occurrence-first; old /events/1-4 hardcoded fakes gone — correct).
- /events/[id] un-hardcoded: CMS-driven, notFound() fallback. NOTE: site-wide quirk (pre-existing, teams/media too) — dynamic notFound pages return HTTP 200 with 404 UI; random unmatched routes 404 properly.
- EventsPanel: event_type/visibility fields, occurrence write-through on save (starts_at = T:00+02:00, all_day = !time, status confirmed), cancel/reinstate quick action (sequence+1, confirm dialog), cancelled chip + month-grid strikethrough. Admin page fetches + merges event_occurrences (role-scoped via getAdminCollection params).
- IAM: only super_admin has events tab (editor doesn't) → super_admin bypasses collection grants (all:true); read-token grant for event_occurrences = permission id 38; revalidate flow already includes event_occurrences.
- Verified: tsc clean, 27/27 vitest, lint clean, build green (webpack), deployed 
px vercel --prod --yes → https://zimrugby.vercel.app. Live E2E: calendar-sso-e2e.mjs 18/18, phase1 12/12, ws2 10/10 (flake on first run = revalidate propagation, re-run passes).
- Queued next: remaining admin improvements + stub-panel consolidation (hero_slides/partners) per ADMIN-AUDIT-PLAN.md.
## Session 2026-08-17 pm — Announcement schema consolidation DONE — commit 501d947
- Audit-plan row 'stub-panel consolidation (hero_slides/partners)' verified ALREADY DONE (0aeac6c): SponsorsPanel + HeroLayoutPanel full CRUD, click route queries partners not sponsors, /partners CMS-driven, proxy has GET + audit logging. The genuinely-open item was the canonical announcement schema: 7 parallel interfaces existed.
- Consolidated 3 wire-schema duplicates onto canonical Announcements (directus-generated.ts, the documented single source of truth): api/announcements.ts local DirectusAnnouncementItem deleted; data-fetcher.ts legacy DirectusAnnouncement (fake category/date/urgent fields) deleted + mapping fixed to real prod fields (starts_at→date, badge/priority→category; previously every announcement rendered 'Just now'/'Official Announcement'); occurrences.ts local subset deleted. Also DirectusSocialPost→SocialPosts and DirectusNewsLookup→DirectusNewsItem in data-fetcher. UI-local prop types (PinnedAnnouncements, TeamMediaSection) kept - component props, not wire schemas.
- Verified: tsc clean, 27/27 vitest, lint clean (pre-existing warning only), build green, deployed live: /media 200 with announcement category labels, /api/calendar 200 (announcement occurrences intact).
- Queued next (audit plan, still open): proxy hardening rows already done (GET+audit+soft-delete); remaining = none major — optional: custom CMS domain (parked by Ed), Upstash KV enable (awaits Ed's creds).


## Session 2026-08-17 late — Asset proxy least-privilege + missing collections DONE — commit c9755d6
- /api/assets/[id] proxy now uses DIRECTUS_READ_TOKEN (fallback DIRECTUS_TOKEN), never admin token — public asset proxy can't escalate to admin writes (audit-plan security finding). Live round-trip verified: upload -> site proxy 200 -> cleanup.
- Directus 12 gotcha: /assets 403s with read token UNLESS the policy has a directus_files read grant — added (permission id 41).
- footer_navigation + referee_resources collections CREATED on prod (were missing entirely — Postgres migration gap; site code + admin panels referenced them and got 403 at build -> local fallback). Fields match admin forms (column_title/links for footer; title/category/description/size/download_url/file for referee resources), soft-delete columns included, read grants ids 39/40. Empty (0 rows) — hardcoded fallbacks still render. scripts/create-missing-collections.mjs = repeatable create+verify (8/8).
- Verified: tsc clean, 27/27 vitest, build green, deployed; site /api/assets 200 with new code, admin + read-token queries for both collections 200.
- Remaining audit-plan items: all done. Optional/parked: Upstash KV enable (awaits Ed creds), custom CMS domain (parked), WS5/WS6 roadmap (unscheduled).


## Session 2026-08-18 — Signup routing fixed + The Touchline admin redesign quick wins DONE — commit 901e70c
- Policy (Ed): everyone who signs up is a FAN until an admin with privileges elevates them. All public signups now land in Supabase fan_zone_members (Fan Zone tab) + Directus onboarding_submissions (Enquiries detail kept); CDPA consent checkbox required on SignupForm (submit disabled until checked, zod refine in /api/signup).
- ROOT CAUSES FOUND (both admin tabs silently empty since forever): (1) Supabase fan_zone_members has NO vip_code column -> listFanZoneMembers() select 400'd -> silently [] ; (2) onboarding_submissions table does NOT exist in Supabase (404 on insert, route logged nothing) — Directus has the collection with snake_case fields (full_name/email/phone/role/organization/submitted_at). Directus write previously sent camelCase + NO auth header -> silently dropped.
- Fixes: admin.ts listFanZoneMembers() drops vip_code; listOnboardingSubmissions() now reads Directus (DIRECTUS_TOKEN, snake_case mapping, deleted_at null filter, sort -submitted_at, Supabase fallback); /api/signup Directus write = snake_case + Bearer DIRECTUS_TOKEN + error-checked (warn, not silent). Supabase project gxoieijhqjhrbykxdguv.supabase.co; service-role key in %TEMP%\opencode\session_dump.txt.
- The Touchline redesign (spec: ZIM RUGBY UNION\THE-TOUCHLINE-ADMIN-REDESIGN.md, workspace only): tab IDs UNCHANGED (gating intact), labels only. Sidebar: Home (Today, Drafting Assistant), Write & Publish (News & Stories), Site & Audience (Homepage & Banners, Website Pages, Events & Festivals, Resources, Sponsors & Partners, Clubs & Development, Help & Footer), Matches (Fixtures & Results), Fans & Partners (Fan Zone, Enquiries), Team & Safety (Team & Permissions, Activity Log, Backups & Restore); footer = THE TOUCHLINE / ZRU Digital. AdminClient top nav mirrors. AI Writer -> Drafting Assistant (AiAssistantPanel, RolesPanel, CommandPalette); Purge CDN Cache -> "Refresh live site"; BackupsPanel fully plain-English (safe points, download backup, integrity check, speed test, restore); StatusChip maps statuses (published->Live, scheduled->Scheduled, pending/review->Review); undo-publish toast 5s on bulk publish (ToastProvider gained durationMs in ToastAction).
- Verified: tsc clean, 27/27 vitest, build green, deployed to prod. Live E2E: signup no-consent 400 / with-consent 200 / Supabase row w/ cdpa_consent / Directus row snake_case / admin login -> 9/9 PASS; admin /admin HTML contains probe rows in BOTH tabs (Fan Zone + Enquiries) — the original bug is dead; new nav labels confirmed in deployed admin chunks. Probe rows cleaned: Supabase test emails deleted (only edwitter123magejo@gmail.com remains), Directus onboarding 0 rows.
- Scripts (TEMP, reusable): signup-routing-e2e-v2.mjs, admin-tabs-visibility.mjs, chunk-label-check.mjs, cleanup-signup-probes.mjs.

## Session 2026-08-18 - The Touchline redesign medium wins 7-8 + 12 DONE - commit b99bb24 (preview)
- Pipeline (win 7): ConfirmProvider gained prompt()/usePrompt (textarea modal for editor feedback); StatusChip tones+labels (in_review->amber "In review", approved->teal "Approved", published->green "Live", archived->dark "Archived"); CollectionManager reviewable/canReview props, pipeline toggleStatus (draft<->in_review, published/active/approved->draft; publish gates on audience-naming confirm + 5s undo toast), approveItem (sets approved, clears review_note), requestChanges (prompt -> draft + review_note shown under row title), bulk publish confirm names audience; AdminClient role prop, canReview = super_admin|editor; news + campaigns reviewable with status options [draft,in_review,approved,published]/[draft,in_review,approved,active,published]; page.tsx passes role={session.role}; TodayOverview review queue card ("Needs your review"/"In review", in_review news items -> media tab); MatchCentrePanel saveScore second confirm when existing result scores change (names teams + old/new).
- Publish wizard (win 8): ArticleComposer now 3-step (Story -> Write -> Post) with step indicator, checklist (headline>5 chars, category set, image) gating "Post now", buttons: Save as draft / Send for review (in_review) / Post now (published, audience confirm + undo toast w/ 5s), schedule via publish_at, posted success panel (View live page / Write another / Undo via PATCH status draft), word count, preview modal kept.
- Safe-point timeline (win 12): BackupsPanel sessionCheckpoint -> sessionCheckpoints[] (sessionStorage zru_session_checkpoints, max 10), auto "Session start" baseline, "Save a new safe point" button, human labels (just now/Xm ago/time), per-point "Revert to this" (confirm + PATCH all records from snapshot).
- review_note TEXT field added to news + campaigns on prod Directus (API).
- Verified: tsc clean, 27/27 vitest, build green, deployed. Live E2E: created probe news in_review -> admin login 200; 9/9 deployed chunk strings (Needs your review, wizard buttons, Safe point timeline); pipeline transitions via API approved->published->draft->published all OK; probe deleted (id 47). Remaining wins: 9 matchday mode, 10 live preview (composer already has phone/desktop toggle), 11 roles redesign.

## Session 2026-08-18 - The Touchline redesign wins 9-11 DONE - commit 223edb1 (preview) - REDESIGN COMPLETE (all 12)
- Matchday mode (win 9, MatchCentrePanel): pinned card on Fixtures & Results — picks next/live/recent match from initialMatches, 30s-ticking countdown (d/h/min) to kickoff, checklist derived live: Result sheet filled in (scores entered), Match report on the site (news title/excerpt mentions home or away team, fetched from news collection once), Match Day photos uploaded (news w/ gallery/photos/album within 14d); buttons: Enter result / live score (opens the existing Live Operator, scrolls to it), Draft the match report (creates draft news 'X vs Y — match report' + toast), Add fixture (scrolls to create form). Honest 3/3 state + remaining hint copy.
- Roles redesign (win 11, RolesPanel): plain-language capability preview in the actor editor — "This actor will be able to" (CheckCircle2, capped 10 + "+n more") / "will NOT be able to" (Ban, capped 6) derived from tabs/collections/features; 4 one-click templates (Content Editor, Media Manager, Fan & Enquiry Support, Viewer read-only); sensitive-access confirms via shared ConfirmProvider (was window.confirm): deleteRole danger ("users lose admin access immediately"), assignUserRole '' (remove access) + super_admin (full control incl. permanent delete + fan PII), disableMfa danger. FEATURE_LABEL Partial<Record> map for human copy (pages_builder/ai_assistant/media_upload/fanzone_pii).
- Live preview (win 10, ArticleComposer): preview modal gained "As it appears in the homepage Latest News panel" card — image (or placeholder), category chip, line-clamped headline/excerpt, date — alongside the existing desktop/mobile article view + step-3 checklist (wizard's step 4 was already covered by the checklist gating Post now).
- Verified: tsc clean, 27/27 vitest, build green, deployed to prod; live chunk check 14/14 (Matchday Mode, checklist strings, templates, capability preview, sensitive-access copy, homepage card preview). Redesign complete: wins 1-6 (quick, 901e70c), 7-8+12 (b99bb24), 9-11 (223edb1).
